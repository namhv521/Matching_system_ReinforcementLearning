# ── Core ──────────────────────────────────────────────────────
python-dotenv==1.0.1
pydantic==2.7.4
pandas==2.2.2
tqdm==4.66.4


# ── PDF / OCR ─────────────────────────────────────────────────
pypdf==4.3.1
python-docx==1.1.2

# OCR fallback (optional — install only if PDFs are scanned)
pdf2image==1.17.0
pytesseract==0.3.13
Pillow==10.4.0
# Also requires: Tesseract binary + vie.traineddata language pack

# ── Gemini API ────────────────────────────────────────────────
# google-genai 2.x có bug với pydantic 2.7.x — dùng 1.16.0
# gemini-2.0-flash đã bị retire (Aug 2026), dùng gemini-2.5-flash
google-genai==1.16.0

# ── Web crawling ──────────────────────────────────────────────
requests==2.32.3
beautifulsoup4==4.12.3
lxml==5.2.2

# ── NLP / Embedding (Phase 2) ─────────────────────────────────
scikit-learn==1.5.1
# sentence-transformers==3.0.1
# faiss-cpu==1.8.0

# ── RL (Phase 3) ──────────────────────────────────────────────
gymnasium==0.29.1
stable-baselines3==2.3.2
sb3-contrib==2.3.0
# torch==2.3.1

# ── Backend (Phase 4) ─────────────────────────────────────────
# fastapi==0.111.0
# uvicorn[standard]==0.30.1
# sqlalchemy==2.0.30
# alembic==1.13.1
# psycopg2-binary==2.9.9

# ── Experiment tracking ───────────────────────────────────────
# mlflow==2.13.2
# tensorboard==2.17.0
